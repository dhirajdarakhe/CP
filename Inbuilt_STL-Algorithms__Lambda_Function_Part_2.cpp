#include <bits/stdc++.h>
using namespace std;
int main()
{
    // auto sum = [](int x, int y)
    // { return x + y; };
    // cout << sum(12, 34);
    // cout << "Dhirak";
    vector<int> v = {2, 4, 2, 5, 7, 8};
    // cout << all_of(v.begin(), v.end(), [](int x)
    //                { return x % 2 == 0; }); // 1 if( all are even ) else 0
    // cout << any_of(v.begin(), v.end(), [](int x)
    //                { return x % 2 == 0; }); // 1 if( at there is one even element ) else 0
    // cout << none_of(v.begin(), v.end(), [](int x)
    //                { return x % 2 == 0; });  // confusion

    // string s = "";
    // s.push_back('s');
    // s.push_back('s');
    // s.push_back('s1');
    // s.push_back('s1');
    // s.pop_back();
    // s.push_back('s');
    // s.push_back('s');
    // cout << s << endl;
    return 0;
}