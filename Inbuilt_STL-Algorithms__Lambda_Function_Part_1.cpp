#include <bits/stdc++.h>
using namespace std;
int main()
{
    // // vector<int> v1(5);
    // // for (int i = 1; i < 5; i++)
    // // {v1[i] = i * i;}

    // ///OR
    
    // vector<int> v1;
    // for (int i = 1; i < 5; i++)
    // {v1.push_back(i  * i);}


    // int min = *min_element(v1.begin(), v1.end());
    // cout << min << endl;
    // int max = *max_element(v1.begin(), v1.end());
    // cout << max << endl;
    // int ct = count(v1.begin(), v1.end(), 14);
    // cout << ct << endl;
    // int sum = accumulate(v1.begin(), v1.end(), 0);
    // cout << sum << endl;
    // for(auto s : v1){
    //     cout<<s<<" ";
    // }cout<<endl;
    // reverse(v1.begin(), v1.end());
    // for(auto s : v1){
    //     cout<<s<<" ";
    // }cout<<endl;
    // string s = "DHIRAJDARAKHE";
    // cout<<s<<endl;
    // reverse(s.begin(), s.end());
    // cout<<s<<endl;










    
    return 0;
}